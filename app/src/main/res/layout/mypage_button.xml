<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/mypage_list_click"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@color/white">

        <TextView
            android:id="@+id/button_string"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/notosans_bold"
            android:includeFontPadding="false"
            android:layout_marginHorizontal="40dp"
            android:layout_marginVertical="12dp"
            android:text="옵 션"
            android:textColor="@color/black"
            android:letterSpacing="-0.03"
            android:textSize="12sp"/>
    <TextView
        android:layout_below="@id/button_string"
        android:id="@+id/itemCount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@color/invisible"
        android:layout_alignEnd="@id/button_string"
        android:layout_alignStart="@id/button_string"
        android:fontFamily="@font/notosans_regular"
        android:includeFontPadding="false"
        android:gravity="center"
        android:text="0"
        android:textColor="@color/main"
        android:letterSpacing="-0.03"
        android:textSize="12sp" />

    <View
        android:id="@+id/side_line"
        android:layout_width="2dp"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/button_string"
        android:layout_alignTop="@id/button_string"
        android:layout_above="@id/itemCount"
        android:background="@color/gray" />
</RelativeLayout>